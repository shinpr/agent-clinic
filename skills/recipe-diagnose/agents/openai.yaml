interface:
  display_name: "Session Doctor"
  short_description: "Diagnose a past Claude Code or Codex session"
  default_prompt: "Use $recipe-diagnose to diagnose a Claude Code or Codex session. Resolve a supplied selector, or present the most recently active candidate for confirmation when none is supplied."

policy:
  allow_implicit_invocation: false
